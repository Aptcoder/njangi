export enum NjangiInviteStatusEnum {
  pending = 'pending',
  accepted = 'accepted',
  ignored = 'ignored',
}

export enum NjangiDurationEnum {
  weekly = 'weekly',
  monthly = 'monthly',
  biweekly = 'biweekly',
}
